# MyTA homepage composition preview

Run from the repository root:

```powershell
python -m http.server 4234 --bind 127.0.0.1
```

Open:

`http://127.0.0.1:4234/experiments/homepage-composition-v1/`

This isolated preview contains only the approved header, Hero carousel, and Section 2 product story. It does not reference either source experiment at runtime.
