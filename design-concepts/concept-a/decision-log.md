# Concept A decision log

Concept: Connected Assignment Loop

| Choice | Governing source | Why necessary | Alternatives rejected |
|---|---|---|---|
| Primary content width is 1200 px with 24 px gutters. | Design Contract page grid | The contract fixes the desktop target and primary width but not the CSS implementation. | Wider than 1280 px media limit; narrower layout that would reduce product authority |
| Hero uses exact columns 1–5 for copy and 6–12 for product. | Concept A Hero blueprint | The blueprint explicitly assigns the grid. | Centered Hero from Concept B; 5/7 annotated workspace from Concept C |
| Hero headline is 72 px desktop, 58 px tablet, and 42–48 px mobile through the shared clamp. | Design Contract type ranges; Concept A 64–68 px blueprint conflict resolved by higher source order | The Design Contract allows 64–72 px; the rendered width needed three desktop lines without clipping. | Smaller headline that weakens the locked promise; type above 72 px |
| Hero product frame uses the full assigned seven columns and a 16:10 setup placeholder. | Concept A blueprint; Placeholder Spec | Product evidence must dominate the structural path. | Decorative framing, smaller screenshot, or competitor imagery |
| The path is 2 px, blue outward and teal on return. | Concept A signature rules | The blueprint allows 1–2 px and specifies color roles. | 1 px at full-page thumbnail scale; arrows, nodes, glow, animation |
| Problem section uses a light-neutral surface and a 5/7 split. | Concept A Problem blueprint; Design Contract surfaces | The blueprint permits white or light neutral; light neutral separates the bridge without a band. | Dark band; boxed signal cards; decorative illustration |
| Middle-state labels use a two-column text cluster with no individual boxes. | Concept A Problem blueprint | Six labels needed readable density inside the middle stage. | Pills, badges, equal cards, horizontal scrolling |
| Walkthrough stage copy is sticky only above 720 px; frames remain in normal flow. | Concept A Product walkthrough blueprint | Sticky copy preserves stage orientation without producing an empty scroll region. | Sticky mobile behavior; JavaScript stage switching; full-width bands |
| Walkthrough spacing is 128 px desktop and 80 px mobile. | Design Contract spacing scale; walkthrough receives greatest vertical space | The frames need separation while remaining a continuous story. | 96 px desktop, which compressed stage distinction; spacing above 128 px |
| Mobile reorders each stage as copy then product and converts the path to a left-margin vertical line. | Concept A mobile blueprint | The blueprint explicitly requires this order and simplified path. | Mechanical desktop grid shrink; product before heading; horizontal sequence |
| Why section uses a 5/7 editorial split with rules, plus a three-column institutional row. | Concept A blueprint | The row accommodates adoption heading, review copy, and LMS direction without cards. | Feature cards; logo row; full trust section |
| Pilot panel uses 64 px padding desktop and 32×24 px mobile. | Design Contract spacing scale; Concept A CTA blueprint | A contained conclusion needed sufficient hierarchy without becoming oversized. | Saturated blue band; enterprise-style full-bleed CTA |
| Borders are limited to product frames, rules, the contained institutional statement, and CTA boundary. | Design Contract surface rules | Structural separation is necessary in the neutral prototype. | Wrapping every block; shadow-heavy cards; borderless placeholders with unclear bounds |
| Product placeholders show their complete 16:10 layout on desktop and the approved taller student crop on mobile. | Placeholder Spec; Design Contract responsive behavior | Future screenshots can replace the same regions without restructuring. | Cropping away essential labels; fake UI details; 16:10 student frame on narrow mobile |
